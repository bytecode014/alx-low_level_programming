#include "main.h"

/**
 *
 *mul - checks for digits (through 0 and 9)
 *@a: a -  Variable
 *@b: b -  Variable 
 *Return: Always 0
 */

int mul(int a, int b)
{
	int mult;

	mult = a * b;
	return (mult);
}
